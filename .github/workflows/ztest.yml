name: ztest
on:
  push:
      branches: [ main ]
        workflow_dispatch:
        jobs:
          check:
              runs-on: ubuntu-latest
                  steps:
                        - run: echo ok
